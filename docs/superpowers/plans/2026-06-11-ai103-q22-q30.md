# AI-103 Questions 22-30 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Refine AI-103 questions 22 through 30 against the source PDF so the React page preserves case-study structure, answer visuals, and answer summaries accurately.

**Architecture:** Keep the existing AI-103 page and generic renderer intact where possible, add focused parsing helpers only for the new structures in questions 27, 28, and 30, and generate PDF-derived assets only for the visual answer-area content in question 30.

**Tech Stack:** React 19, Vite, plain CSS, JSON content, Node test runner, bundled Poppler `pdftoppm`, bundled Python + Pillow.

---

### Task 1: Extend AI-103 parsing helpers and tests for questions 22-30

**Files:**
- Modify: `allinone/src/utils/learning.js`
- Modify: `allinone/src/utils/learning.test.js`

- [ ] Add failing tests for any new helper behavior required by questions 27, 28, and 30.
- [ ] Run `node --test src/utils/learning.test.js` from `allinone/` and confirm the new tests fail for the expected missing behavior.
- [ ] Implement the minimal parsing helpers and exports needed to support:
  - case-study multiple choice questions 27 and 28
  - visual drag-drop question 30 answer summary extraction
- [ ] Re-run `node --test src/utils/learning.test.js` from `allinone/` and confirm it passes.

### Task 2: Generate PDF-derived assets for question 30

**Files:**
- Create: `allinone/src/assets/ai103/q30-answer-area-blank.png`
- Create: `allinone/src/assets/ai103/q30-answer-area.png`
- Use source renders under: `tmp/pdfs/ai-103-q22-30/`

- [ ] Inspect PDF page 35 and identify the crop boxes for the unsolved and solved answer areas.
- [ ] Generate the assets from `/Volumes/Samsung_T5/Download/ai-103.pdf` using the bundled `pdftoppm` and bundled Python runtime.
- [ ] Verify the cropped assets are readable and tightly framed around the answer areas.

### Task 3: Integrate questions 22-30 into the AI-103 page

**Files:**
- Modify: `allinone/src/pages/AI103.jsx`
- Modify: `allinone/src/pages/AI103.css`

- [ ] Update the page to import and render the new question 30 assets.
- [ ] Add focused question components or configuration for questions 27, 28, and 30 while leaving questions 22-26 and 29 on generic patterns if the PDF structure matches.
- [ ] Keep section headings semantic: `Question`, `Answer Area`, `Correct Answer Area`, `Answer`, `Description`.
- [ ] Keep scrolling and existing pagination behavior intact.

### Task 4: Verify the refined question range

**Files:**
- Verify only; no required code changes

- [ ] Run `node --test src/utils/learning.test.js` from `allinone/`.
- [ ] Run `npx eslint src` from `allinone/`.
- [ ] Run `npm run build` from `allinone/`.
- [ ] Start the dev server if needed and verify `http://127.0.0.1:5173/My-Work/allinone/#/learning/ai-103` in the in-app browser with questions 22 through 30.
