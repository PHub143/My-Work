# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# YBM booklet scans and audio are served from Drive, not the repo. Push a
# test's local assets with api/scripts/upload-ybm-assets.js, which writes a
# small Drive-file-id manifest under src/data/ybm/assets/ (that one IS
# committed) for allinone/src/utils/ybm.js to resolve at runtime.
public/ybm/
